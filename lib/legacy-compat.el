;; Make this configuration compatible with old versions of emacs.
;;
;; Tested with emacs-22 as that's what ships with OSX.
(if (< emacs-major-version 24)
    (progn
      ;; Declare stubs for compilation.
      ))

